package hexlet.code;

import java.nio.file.Path;

public class Differ {
    /*
    var diff = Differ.generate(filePath1, filePath2);
    System.out.println(diff);

    static STRUCT generate(Path file1, Path file2) {

    }

     */
}
